import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "OAuth Demo",
  description:
    "Experience OAuth authentication firsthand with this demo app, showcasing secure access to GitHub and Google resources. Explore Authorization Code Grant and Implicit Grant functionalities, demonstrating seamless integration and user authentication for third-party platforms",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
